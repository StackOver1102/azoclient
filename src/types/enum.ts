export enum StatusEnum {
    SUCCESS = 'success',
    FAIL = 'fail',
    ERROR = 'error',
}

export enum Role {
    'admin' = 'admin',
    'user' = 'user',
    'other' = 'other',
}

export enum TypeHearder {
    HOME = "home",
    OTHE = "other"
}