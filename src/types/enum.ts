export enum StatusEnum {
    SUCCESS = 'success',
    FAIL = 'fail',
    ERROR = 'error',
}

export enum Role {
    'admin' = 'admin',
    'user' = 'user',
    'other' = 'other',
}

export enum TypeHeader {
    HOME = "home",
    OTHER = "other"
}